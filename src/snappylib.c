#include <stdbool.h>

bool compress(string file_path, string file_dest){
    //Comprime il file e ritorna true per ok, false per errore
}



'tipo' getSizeFormatted( int size ){ //TODO tipo di ritorno
    //ritorna dimesione già formattata
    //per essere scritta all'inizio del file 
    //( LSB first, 7 bit per la dimensione + 1 bit
    // messo a 1 se c'è bisogno di altri byte per
    // scriverla )
}
